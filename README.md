# Getting Started with Event Crafter App

## How to run the code

Run `npm install` to install all the required node modules.
Open the code in the code editor on your machine
Open terminal
Run the command `npm start`
